{
  "buildCommand": "npm run fetch-channels || true && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "functions": {
    "src/app/**": { "maxDuration": 30 }
  }
}
